#include <iostream>
#include <cmath>
using namespace std;

int main() {
    cout << "Hello, World!";
    double x = 3.14;
    double y = 4;
    double z;

    x = max(x,y); // max() to find max of number
    double minimum;
    minimum = min(x,y); // to find minimum of a number
    double power;
    power = pow(2,3); // pow(base,power) is used to power a number
    double sqrroot;
    sqrroot = sqrt(9); // it finds square root of a number
    int pos_neg;
    pos_neg = abs(-5); // will convert positive to neg and neg to positive
    int roundup;
    roundup = round(x); // used to round up a number
    
    
    return 0;
}