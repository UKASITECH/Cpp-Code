#include <iostream>
using namespace std;

typedef int lol;
typedef double print;
int main() {
    // typedef are reserevd keyword used to create an additional name(alias) for another datatype
    // new identifier for an existing type
    // helps with readabillity and reduce typos
   
    lol umang = 1;
    cout << umang;

    // here we converted int to lol using typedef function
    print kepler;
    return 0;
}